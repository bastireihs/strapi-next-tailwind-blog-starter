export enum HttpMethod {
  GET = 'GET',
  POST = 'POST',
  PUT = 'PUT',
  DELETE = 'DELETE',
}

export enum Color {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  SUCCESS = 'success',
  DANGER = 'danger',
  WARNING = 'warning',
  INFO = 'info',
  LIGHT = 'light',
  DARK = 'dark',
  WHITE = 'white',
  BLACK = 'black',
  GRAY = 'gray',
}

export enum Position {
  TOP = 'top',
  RIGHT = 'right',
  BOTTOM = 'bottom',
  LEFT = 'left',
  END = 'end',
  START = 'start',
  CENTER = 'center',
  MIDDLE = 'middle',
}

export enum Shape {
  SQUIRCLE = 'squircle',
  HEXAGON = 'hexagon',
  TRIANGLE = 'triangle',
}

export enum Size {
  SM = 'sm',
  MD = 'md',
  LG = 'lg',
  XS = 'xs',
}

export enum Status {
  DRAFT = 'DRAFT',
  PUBLISHED = 'PUBLISHED',
}

export enum ButtonType {
  BUTTON = 'button',
  SUBMIT = 'submit',
  RESET = 'reset',
}

export enum CardVariant {
  NORMAL = 'normal',
  COMPACT = 'compact',
  SIDE = 'side',
}

export enum Theme {
  LIGHT = 'light',
  DARK = 'dark',
}
